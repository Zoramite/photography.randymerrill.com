extends layout

block prepend title
  | #{ page.title } - 
  
block content
  include author

  h2= page.title

  article.post
    section!= page.html
  if page.metadata.gallery
    include gallery

    mixin gallery(page.metadata.gallery)
